CREATE TABLE "SWFT01UA1"."TB_RALF_CUSTOMER" 
   (	
    "CUSTOMER_ID" VARCHAR2(30) NOT NULL ENABLE, 
	"FIRST_NAME" VARCHAR2(30), 
	"MIDDLE_NAME" VARCHAR2(30), 
	"LAST_NAME" VARCHAR2(30), 
	"CREATE_DATE" DATE, 
	"LAST_UPDATE_DATE" DATE, 
	"DATE_OF_BIRTH" DATE, 
	"DETAIL" CLOB, 
	 PRIMARY KEY ("CUSTOMER_ID")
 )  
 
  ALTER TABLE "SWFT01UA1"."TB_RALF_CUSTOMER" MODIFY ("CUSTOMER_ID" NOT NULL ENABLE);
 
  ALTER TABLE "SWFT01UA1"."TB_RALF_CUSTOMER" ADD PRIMARY KEY ("CUSTOMER_ID")
  
  /

CREATE OR REPLACE TRIGGER "SWFT01UA1"."CREATE_RALF_CUSTOMER" 
BEFORE INSERT
ON TB_RALF_CUSTOMER
REFERENCING NEW AS NEW OLD AS OLD
FOR EACH ROW
BEGIN
   :NEW.CREATE_DATE := SYSDATE;

   EXCEPTION
     WHEN OTHERS THEN
       -- Consider logging the error and then re-raise
       RAISE;
END;



/
ALTER TRIGGER "SWFT01UA1"."CREATE_RALF_CUSTOMER" ENABLE;